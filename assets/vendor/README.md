## XLSX export dependency

This project generates real `.xlsx` files in the browser using SheetJS (`XLSX`).

Place `xlsx.full.min.js` here:

- `assets/vendor/xlsx.full.min.js`

You can get it from SheetJS (or a CDN) and keep it in this folder so the app works without external network access.

